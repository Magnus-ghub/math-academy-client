export enum PaymentType {
  PREMIUM = "PREMIUM",
  GROUP = "GROUP",
}

export enum PaymentProvider {
  CLICK = "CLICK",
  MANUAL = "MANUAL",
}

export enum PaymentStatus {
  PENDING = "PENDING",
  CONFIRMED = "CONFIRMED",
  FAILED = "FAILED",
}